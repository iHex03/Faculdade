/*#include <stdio.h>
#include <stdlib.h>

int potencia(int x, int n, int i)
{
    int m=x;

    if(i<n)
    {
        i++;
        return m*=potencia(x, n, i);
    }

    return 1;
}

int main()
{
    int x, n, pot;
    printf("Digite o X: ");
    scanf("%d", &x);
    printf("\nDigite o n: ");
    scanf("%d", &n);
    pot = potencia(x, n, 0);
    printf("\n--------------\nPotencia: %d\n--------------\n", pot);
    return 0;
}*/
